

tmp = stb;
while (tmp)
{
	printf("%d ", tmp->data);
	tmp = tmp->next;
}
pb(&stb, &sta);
pb(&stb, &sta);
pb(&stb, &sta);
pb(&stb, &sta);
pb(&stb, &sta);
pb(&stb, &sta);
pb(&stb, &sta);
printf("\n");
tmp = stb;
while (tmp)
{
	printf("%d ", tmp->data);
	tmp = tmp->next;
}